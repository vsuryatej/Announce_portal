{
  "name": "announcement-portal",
  "version": "1.0.0",
  "private": true,
  "description": "Internal announcement and communication portal",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:setup": "npm run db:setup --prefix server",
    "db:reset": "npm run db:reset --prefix server",
    "db:check": "npm run db:check --prefix server",
    "db:studio": "npm run db:studio --prefix server",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  }
}
